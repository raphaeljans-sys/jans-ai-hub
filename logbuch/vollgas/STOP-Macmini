STOP fuer den Endlos-Runner auf dem Mac Mini — gesetzt 27.07.2026 22:15
(Freigabe Raphael, Leerlauf-Bereinigung nach dem 35-Stunden-Totalausfall).

GRUND: Der Mini-Runner hatte nichts mehr zu tun und meldete seit Stunden im
Zehn-Minuten-Takt «Keine Trainings-Tasks gefunden unter ~/.claude/scheduled-tasks».
Ursache verifiziert: saemtliche Trainings-Tasks auf dem Mac Mini tragen
`enabled: false` in der SKILL.md-Frontmatter und werden vom Runner (Zeile 131)
korrekt uebersprungen:

  baurecht-buch-training      enabled: false
  energie-training            enabled: false
  normen-training-mini        enabled: false
  planungsgrundlagen-training enabled: false
  synobsis-batch-nacht        enabled: false  (zusaetzlich in EXCLUDE_RE)
  grobkosten-training         in EXCLUDE_RE
  claude-abo-auslastung       erfuellt INCLUDE_RE nicht

Damit bleibt die Task-Liste leer, und der Prozess drehte endlos leer. Er verbrauchte
dabei keine Tokens, verschleierte aber, dass der Mac Mini nichts mehr beitraegt.

DIESE DATEI ENTFERNEN, sobald auf dem Mac Mini wieder mindestens ein Trainings-Loop
aktiv geschaltet ist (Frontmatter `enabled: true`). Danach:
  ssh mini 'nohup bash /Volumes/daten/jans-ai-hub/scripts/vollgas-runner.sh >/dev/null 2>&1 &'

Der Mac Mini traegt weiterhin ueber seine App-Scheduled-Tasks bei; nur der
Endlos-Runner ruht.

--------------------------------------------------------------------------
KORREKTUR 28.07.2026 01:07 (vollgas-chef-radar): Der Satz oben, der Mac Mini
trage "weiterhin ueber seine App-Scheduled-Tasks bei", ist unvollstaendig und
in der Sache irrefuehrend. Neben Registry und Endlos-Runner existiert ein
DRITTER Feuermechanismus, den weder die Registry noch die STOP-Dateien
erreichen: zwei geladene launchd-Jobs, die ueber cron-training-mini.sh ->
dispatch-run.sh -> claude -p mit 25 USD Budget feuern.

  ch.jans.training-plg       taeglich 00:30   -> ABGESCHALTET 28.07.2026
  ch.jans.training-energie   taeglich 22:30   -> laeuft weiter (produktiv)

Sie sind der Grund, weshalb planungsgrundlagen (Run 92) und energie (Run 117)
in der Nacht auf den 28.07. gelaufen sind, obwohl beide Loops seit dem 26.07.
als stillgelegt galten.

ABGESCHALTET wurde nur ch.jans.training-plg: 27 Erschoepfungsbestaetigungen in
Folge (Run 67-92), Schwelle des Leerlauf-Auftrags liegt bei 5. Vollzug per
launchctl bootout, plist umbenannt nach
  ~/Library/LaunchAgents/ch.jans.training-plg.plist.disabled-260728
Reaktivierung: zurueckbenennen und
  launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/ch.jans.training-plg.plist

ch.jans.training-energie bleibt bewusst aktiv (Run 117 lieferte vier neue
Themenfelder, kein Leerlauf).

MERKE fuer jede kuenftige Takt- oder Stilllegungsentscheidung auf dem Mini:
es sind DREI Orte nachzuziehen, nicht zwei -- Registry, Runner-Frontmatter/
EXCLUDE_RE UND die launchd-Jobs in ~/Library/LaunchAgents.
--------------------------------------------------------------------------
