# QUESTIONS-Abarbeitung, sechster Lauf (23.08.2026)

**Auftrag:** weitere offene Fragen in `wiki/QUESTIONS.md` abarbeiten, Fundstelle je Aussage
(Norm, Ausgabe, Ziffer), Verifikationsstatus des Destillats vor Zitat lesen.

## Ergebnis

1. **BRL 13-15 Doppelbestand (260823) — geschlossen, ohne Löschen/Mergen.** Kanon-Frage über
   established > speculative (Rule `normen-referenz` Ziff. 1b) entschieden. Beide Destillate
   tragen jetzt einen gegenseitig verlinkten Kanon-Hinweis im Fliesstext. Kein Inhalt entfernt.
2. **N57-3 (Korrigenda SIA 416/1:2007) — logisch geschlossen.** Norm ist seit 31.03.2015
   archiviert (N57-2); Korrigenda-Prüfung einer archivierten Ausgabe ohne praktische Wirkung.
3. **N59-4 (40-15 Zwischenausgaben) — Methode nachgeschärft, Befund unverändert offen.**
   Strukturierte JSON-Feldauswertung statt Text-Grep bestätigt: genau ein Archiv-Titel-Treffer,
   kein «(gültig bis …)»-Zweiteintrag wie bei 108-15. Bleibt offen, keine weitere freie Quelle.

## Bewusst nicht weiterverfolgt

- N-SIASWEEP-1 bis -4: heute bereits siebenfach über unterschiedliche Quellenarten erschöpft
  (Shop-Produktseite, zwei amtliche SIA-Kataloge, Espazium, SIA-Webinar-Seite,
  SNV-Arbeitsprogramm, Wayback Machine, Cloud-weite Dateisuche) — kein Grenzertrag zu erwarten.
- N59-2 (Wasserzeichen 40-15): bereits nachrangig, Werkzeug-Lehre dokumentiert.
- N58-4: Scope-Notiz, keine offene Sachfrage.
- N60-1 (AFC-Synopse-Duplikat), N60-2 (Methodik-Pflicht-Vorschlag): Freigabe Raphael ausstehend,
  nicht selbständig zu entscheiden (destruktiv bzw. neue Verfahrensregel).
- 260731 (NIN Geschirrspüler): Web-Recherche bereits zweifach erschöpft, Volltext hinter
  kostenpflichtigem Zugang (Bring-Schuld Raphael).

## Werkzeug-Hinweis

Tailscale-Hostname `diskstation918.tail8265aa.ts.net` war von dieser Station aus nicht
auflösbar (`ssh: Could not resolve hostname`); die LAN-IP `192.168.1.10` funktionierte für die
native `git diff --numstat`-Prüfung ohne Weiteres. Bei Bedarf zuerst LAN-IP versuchen, wenn der
Tailscale-Name scheitert.

## Verifikationsstand

Alle Aussagen oben stützen sich auf bereits in der KB primärquellenfest belegte Befunde
(N57-2 für Punkt 2) bzw. auf ein bestehendes, dokumentiertes KB-Kriterium (Punkt 1) bzw. auf
eine frische, strukturierte Nachprüfung derselben Primärquelle (Punkt 3). Kein neuer
unbelegter Claim.
