---
titel: Buch-Run 129 — connectors/WEGE.md-Nachtrag für die entscheidsuche.ch-Such-API (Buch-Run 126/127)
datum: 2026-08-24
---

# Buch-Run 129: Wege-Register-Nachtrag entscheidsuche.ch

## Ausgangslage

Die Reglemente-Queue 2414 Thalwil (T1-T9) ist laut CHANGELOG seit Run 87 vollständig
abgearbeitet; die Läufe bis 127 haben Nachträge und Re-Verifikationen erledigt. Buch-Run 127
nannte als einen von drei möglichen nächsten Schritten einen `connectors/WEGE.md`-Nachtrag für
die in Run 126/127 rekonstruierte interne Such-API von `entscheidsuche.ch` — bislang nur in den
KB-eigenen `outputs/`-Reports dokumentiert, nicht im hub-weiten Wege-Register.

Beim Start dieses Laufs liefen bereits zwei weitere Instanzen desselben Prompts
(`mschub706`, gestartet 07:21; `mschub710`, gestartet 07:28) aktiv an derselben KB. Um keine
gleichzeitige Bearbeitung derselben Wiki-Artikel zu riskieren, wurde bewusst eine Aufgabe
ausserhalb der KB-Wiki-Dateien gewählt — genau der von Run 127 selbst als «ausserhalb des
KB-Mandats» eingeordnete dritte Kandidat. Während dieses Laufs hat eine der beiden parallelen
Instanzen Run 128 (Kostenentscheid-Abschnitt in [[nebenbestimmungen-und-reverse]]) committet;
das bestätigt, dass die Trennung sinnvoll war.

## Vorgehen

Keine neue Recherche — die Fakten waren bereits durch Run 126/127 verifiziert
(`wissen/baurecht/outputs/2026-08-24_buch-run126.md`,
`wissen/baurecht/outputs/2026-08-24_buch-run127.md`,
`wissen/baurecht/raw/260824_amtlich_zh_entscheidsuche-abdeckung-vb-1994.md`). Übertragen wurden:

- Endpunkt `https://entscheidsuche.ch/_searchV2.php` (POST/JSON, öffentlich erreichbarer
  Elasticsearch-Proxy, aus dem JS-Bundle der Website rekonstruiert).
- `term`-Query auf das Feld `reference`, indiziertes Format mit **führendem Leerzeichen**
  vor dem Aktenzeichen (Fallstrick, sonst 0 Treffer trotz existierendem Fall).
- `content_url` im Treffer verweist auf statisches, per `curl` abrufbares Volltext-HTML.
- Belegte Abdeckungsgrenze: Index für `hierarchy: ZH_VG` praktisch leer vor 2000 (1991-1995:
  0 Treffer, auch 1994 einzeln bestätigt), ab 2000 vollständig (219+ Treffer/Jahr).

Format des neuen Registereintrags an den bestehenden `zentraleserien.zh.ch`-Nachtrag im
selben Dokument angelehnt (Anlass, Weg, Testbefehl, Falle, Grenze, Quelle).

## Änderungen

- **GEÄNDERT** `connectors/WEGE.md`: neuer Abschnitt «Nachtrag 24.08.2026 —
  ZH-Gerichtsentscheide ohne bekanntes Datum: interne Such-API von `entscheidsuche.ch`»
  direkt nach dem bestehenden `zentraleserien.zh.ch`-Abschnitt eingefügt.
- **GEÄNDERT** `wissen/baurecht/CHANGELOG.md`: dieser Eintrag (Run 129) oberhalb von Run 128
  eingefügt.
- Kein `raw/`-, `wiki/`- oder `buecher/`-Edit in der KB selbst — reine Registerpflege
  ausserhalb des KB-Mandats, wie von Run 127 vorgesehen.

## `git diff --numstat`-Kontrolle

Nach dem Schreibvorgang geprüft (nativ, SMB-Mount, vor dem nächsten `nas-selfcommit`):

- `connectors/WEGE.md`: +37/-0
- `wissen/baurecht/CHANGELOG.md`: +79/-0 (enthält zusätzlich den zwischenzeitlich von einer
  parallelen Instanz eingefügten Run-128-Block — beim Gegenlesen des Diffs erkannt, keine
  Überschreibung, reine Addition oberhalb)

Keine unerwarteten Löschungen.

## Nächster Schritt

Kein Fachpunkt aus der Reglemente-Queue oder ihren Nachtragsketten offen. Für einen künftigen
Lauf bleiben die von Run 127/128 genannten Kandidaten: regulärer `wissenscheck`-Turnus, oder
eine Modell-D-Stichprobe an [[ausnuetzungsuebertragung]]/[[nutzungsziffern]] (zuletzt
2026-07-25/Run 55, ältester `verifiziert`-Stand unter den established-Artikeln).
