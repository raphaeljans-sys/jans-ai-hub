Synchronisiere den JANS AI Hub mit GitHub:

1. Zeige den aktuellen Git-Status (untracked files, changes)
2. Wenn es Änderungen gibt:
   - Stage alle relevanten Änderungen (NICHT .env, .mcp.json oder andere sensible Dateien)
   - Erstelle einen Commit mit einer kurzen, beschreibenden Message auf Deutsch
   - Pushe zum Remote
3. Wenn es keine Änderungen gibt, melde "Alles synchron ✅"
4. Zeige am Ende den Status: letzter Commit, Branch, Remote-Status

Frage NICHT nach Bestätigung — führe den Sync direkt aus.
