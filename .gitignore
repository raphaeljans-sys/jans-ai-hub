# Secrets - NIEMALS committen
.mcp.json
.env
*.secret
*.key

# Node
node_modules/
package-lock.json

# macOS
.DS_Store

# Claude (pro Maschine, nicht synchronisieren)
.claude/*
# Skills werden synchronisiert (sind Code, nicht Config)
!.claude/skills/
